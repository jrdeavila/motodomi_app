import 'package:motodomi_app/lib.dart';

class TechnicalReviewPage extends StatelessWidget {
  const TechnicalReviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DomiciliaryRequestRoutes'),
      ),
      body: const Center(
        child: Text('Scaffold Body'),
      ),
    );
  }
}
